# Shell Scripting

Create and open setup.sh file in vim editor: <br />
`vim setup.sh`

In setup.sh file:
```sh
#!/bin/bash

echo "Setup and configure server"

# save file with 
ESC :wq 

# make file executable
$ chmod u+x setup.sh

# execute script
$ ./setup.sh 
$ bash setup.sh
```